rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // อนุญาตให้ อ่าน และ เขียน ข้อมูลได้ทุกอย่างแบบถาวร
    match /{allPaths=**} {
      allow read, write: if true;
    }
    
  }
}
